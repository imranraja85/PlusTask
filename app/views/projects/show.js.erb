$('#project-contents').html("<div class='project-container'><%= escape_javascript(render :partial => 'milestones') %></div>");
$("#menu-milestones").addClass("active"); //this is used if we hit this url from a page other than the project show page
