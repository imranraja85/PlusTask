$('#project-contents').html("<div class='project-container'><%= escape_javascript(render :partial => 'tasks/tasks') %></div>");
$("#menu-tasks").addClass("active"); //this is used if we hit this url from a page other than the project show page
