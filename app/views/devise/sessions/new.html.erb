<!--
<div class="header">
  <%= image_tag "new-logo.png", :class => 'header-logo' %>
  <div class="clear"></div>
</div>
-->

<div id="login-container">
  <div style="margin-left: 100px">
    <%= image_tag "new-logo.png", :class => 'login-logo' %>

    <%= form_for(resource, :as => resource_name, :url => session_path(resource_name), :class => 'login') do |f| %>
      <p><%= f.label :email %><br />
      <%= f.email_field :email, :value => "example@domain.com" %></p>

      <p><%= f.label :password %><br />
      <%= f.password_field :password %></p>

      <% if devise_mapping.rememberable? -%>
        <p><%= f.check_box :remember_me %> <%= f.label :remember_me %></p>
      <% end -%>

      <p><%= f.submit "Sign in" %></p>
    <% end %>

    <%#= render :partial => "devise/shared/links" %>
  </div>
</div>
